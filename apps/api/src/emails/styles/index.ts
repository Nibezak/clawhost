import darkModeStyles from '@/emails/styles/darkModeStyles'
import main from '@/emails/styles/main'
import container from '@/emails/styles/container'
import heading from '@/emails/styles/heading'
import body from '@/emails/styles/body'
import paragraph from '@/emails/styles/paragraph'
import paragraphMuted from '@/emails/styles/paragraphMuted'
import buttonContainer from '@/emails/styles/buttonContainer'
import button from '@/emails/styles/button'
import logoSection from '@/emails/styles/logoSection'
import logo from '@/emails/styles/logo'

export {
    darkModeStyles,
    main,
    container,
    heading,
    body,
    paragraph,
    paragraphMuted,
    buttonContainer,
    button,
    logoSection,
    logo
}